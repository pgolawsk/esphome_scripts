# This is a test configuration for the Prometheus component in ESPHome.
# Pawelo, 20260218, created

external_components:
  # - source: ../../esphome/esphome/components  # Adjust path to your local repo
  # - source: github://pr#9530  # my prometheus PR from 2025.7.0
    components: [prometheus]
    refresh: 0s  # Always refresh from local source
