---
#* SPI interface on given GPIO pins
# Pawelo, 20240716, created based on https://esphome.io/components/spi#spi

spi:
  id: ${spi_id}
  clk_pin: ${clk}
  type: "single" # could be "single or "quad"
  mosi_pin: ${mosi}
  miso_pin: ${miso}
  interface: "any"