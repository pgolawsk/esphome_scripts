---
#* Intercom connected to GPIO
# Pawelo, 20230219, created as new

# need transistor BC848 and resistor 220Ohm (?)
platform: gpio
pin:
  number: ${gpio} # GPIO2 with inverted
#  mode: INPUT_PULLUP
id: intercom${ix}
name: "$room Intercom${ix}"
#icon: "mdi:gate"
icon: "mdi:doorbel"
state_topic: $devicename/intercom${ix}
on_turn_on:
- delay: $off_delay
- switch.turn_off: intercom${ix}
